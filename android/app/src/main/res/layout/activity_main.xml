<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cs_background">

    <TextureView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Datos en vivo (resolución · fps · Mbps), esquina superior izquierda -->
    <TextView
        android:id="@+id/statsText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="12dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:background="@drawable/bg_pill"
        android:textColor="@color/cs_text"
        android:textSize="11sp"
        android:fontFamily="monospace"
        android:visibility="gone" />

    <!-- Píldora de estado, arriba al centro (tocarla abre el QR) -->
    <LinearLayout
        android:id="@+id/statusPill"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_margin="12dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:background="@drawable/bg_pill"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <View
            android:id="@+id/liveDot"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/dot" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/cs_text"
            android:textSize="13sp"
            android:text="@string/status_stopped" />
    </LinearLayout>

    <!-- Botonera flotante inferior (colores planos de la marca) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="24dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btnBattery"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginEnd="20dp"
            android:background="@drawable/bg_fab_dark"
            android:elevation="6dp"
            android:scaleType="center"
            android:src="@drawable/ic_battery"
            android:tint="@color/cs_on_primary"
            android:contentDescription="@string/battery_button" />

        <ImageButton
            android:id="@+id/btnQr"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginEnd="20dp"
            android:background="@drawable/bg_fab_dark"
            android:elevation="6dp"
            android:scaleType="center"
            android:src="@drawable/ic_qr"
            android:tint="@color/cs_on_primary"
            android:contentDescription="@string/qr_button" />

        <ImageButton
            android:id="@+id/btnStartStop"
            android:layout_width="76dp"
            android:layout_height="76dp"
            android:background="@drawable/bg_fab_solid"
            android:elevation="8dp"
            android:scaleType="center"
            android:src="@drawable/ic_play"
            android:tint="@color/cs_on_primary"
            android:contentDescription="@string/start" />

        <ImageButton
            android:id="@+id/btnSwitchCamera"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginStart="20dp"
            android:background="@drawable/bg_fab_dark"
            android:elevation="6dp"
            android:scaleType="center"
            android:src="@drawable/ic_flip"
            android:tint="@color/cs_on_primary"
            android:contentDescription="@string/switch_camera" />

        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginStart="20dp"
            android:background="@drawable/bg_fab_dark"
            android:elevation="6dp"
            android:scaleType="center"
            android:src="@drawable/ic_tune"
            android:tint="@color/cs_on_primary"
            android:contentDescription="@string/settings_button" />

        <ImageButton
            android:id="@+id/btnOverlay"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginStart="20dp"
            android:background="@drawable/bg_fab_dark"
            android:elevation="6dp"
            android:scaleType="center"
            android:src="@drawable/ic_text"
            android:tint="@color/cs_on_primary"
            android:contentDescription="@string/overlay_button" />
    </LinearLayout>

    <!-- Modo ahorro: capa negra (se activa con el botón de batería) -->
    <View
        android:id="@+id/dimScrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#F2000000"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone" />
</FrameLayout>
